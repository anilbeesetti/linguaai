type ResponseInfoIconProps = {
  isError: boolean;
  isSuccess: boolean;
};
export const ResponseInfoIcon = ({ isError, isSuccess }: ResponseInfoIconProps) => {
  const widthHeight = "w-5 h-5";
  const bgColor = isSuccess
    ? "bg-primary_green_surface"
    : isError
    ? "bg-primary_red_surface"
    : "bg-slate-600";

  return (
    <div className={`w-fit h-fit rounded-full p-1 ${bgColor}`}>
      {isSuccess ? (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          strokeWidth={1.5}
          stroke="currentColor"
          className={`${widthHeight}`}
        >
          <path
            strokeLinecap="round"
            strokeLinejoin="round"
            d="M4.5 12.75l6 6 9-13.5"
          />
        </svg>
      ) : isError ? (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          strokeWidth={1.5}
          stroke="currentColor"
          className={`${widthHeight}`}
        >
          <path
            strokeLinecap="round"
            strokeLinejoin="round"
            d="M6 18L18 6M6 6l12 12"
          />
        </svg>
      ) : (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          strokeWidth={1.5}
          stroke="currentColor"
          className={`${widthHeight} text-white`}
        >
          <path
            strokeLinecap="round"
            strokeLinejoin="round"
            d="M7.5 21L3 16.5m0 0L7.5 12M3 16.5h13.5m0-13.5L21 7.5m0 0L16.5 12M21 7.5H7.5"
          />
        </svg>
      )}
    </div>
  );
};
